image: maven:3.9.9-eclipse-temurin-17

stages:
  - build
  - test

cache:
  paths:
    - .m2/repository

before_script:
  - echo "Using Maven"
  - mvn -version

build:
  stage: build
  script:
    - mvn -B -DskipTests clean package

test:
  stage: test
  script:
    - mvn -B test